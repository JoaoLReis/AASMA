using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public partial class DeliberativeFireFighter : PerceptionInterface
{
    /*public enum BELIEF {NOT_ENOUGH_WATER, NIGHT};
    public enum DESIRE { REST, EXPLORE, LEAD, HELP, RECALCULATE_TARGET, PARTICIPATE, GO_GET_WATER };
    public enum INTENTION { MAP_BUILDINGS, REFILL, REFILL_AND_GO_BACK, HELP_FIRE, LEAD, RECRUIT_HELPER, RECRUIT_HELPER_AND_GO_BACK,  MOVE_TO_TARGET, SLEEP};

    private List<BELIEF> beliefs = new List<BELIEF>();
    private List<DESIRE> desires = new List<DESIRE>();
    private INTENTION plan = INTENTION.MAP_BUILDINGS;

    public void BDIcicle()
    {
        List<BELIEF> currentBeliefs = new List<BELIEF>(beliefs);
        List<DESIRE> currentDesires = new List<DESIRE>(desires);
        if(currentDesires.Contains(DESIRE.REST) && currentBeliefs.Contains(BELIEF.NIGHT))
        {
            plan = INTENTION.SLEEP;
        }
        else if(currentDesires.Contains(DESIRE.LEAD))
        {
            plan = INTENTION.LEAD;
        }
        else if (currentDesires.Contains(DESIRE.EXPLORE))
        {
            if(currentDesires.Contains(DESIRE.LEAD))
            {
                plan = INTENTION.LEAD;
            }
            else if (currentDesires.Contains(DESIRE.))
        }
    }*/
}
